<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>500 — Server Interruption | ELIOR Natural Stones</title>
    <link rel="icon" href="/favicon.ico" sizes="any">
    @fonts
    <style>
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
        body {
            background-color: #0f0f0f;
            color: #d9d6c8;
            font-family: 'Montserrat', system-ui, -apple-system, sans-serif;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 2rem;
            -webkit-font-smoothing: antialiased;
        }
        .brand {
            font-family: 'Playfair Display', Georgia, serif;
            font-size: 1.5rem;
            letter-spacing: 0.28em;
            text-transform: uppercase;
            color: #d9d6c8;
            margin-bottom: 0.5rem;
            text-decoration: none;
        }
        .tagline {
            font-size: 0.65rem;
            letter-spacing: 0.35em;
            text-transform: uppercase;
            color: #b9987a;
            margin-bottom: 4rem;
        }
        .code {
            font-family: 'Playfair Display', Georgia, serif;
            font-size: clamp(5rem, 12vw, 9rem);
            font-weight: 300;
            line-height: 1;
            color: #4a4a4a;
            letter-spacing: -0.04em;
            margin-bottom: 1rem;
        }
        .title {
            font-family: 'Playfair Display', Georgia, serif;
            font-size: clamp(1.75rem, 4vw, 2.5rem);
            font-weight: 400;
            letter-spacing: -0.01em;
            color: #d9d6c8;
            margin-bottom: 1rem;
        }
        .desc {
            max-width: 480px;
            font-size: 0.95rem;
            line-height: 1.7;
            color: #b9987a;
            margin-bottom: 2.5rem;
        }
        .actions {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
            justify-content: center;
        }
        .btn {
            display: inline-block;
            padding: 0.85rem 2rem;
            font-size: 0.75rem;
            letter-spacing: 0.22em;
            text-transform: uppercase;
            text-decoration: none;
            transition: all 0.3s ease;
        }
        .btn-primary {
            background-color: #b9987a;
            color: #0f0f0f;
            border: 1px solid #b9987a;
        }
        .btn-primary:hover {
            background-color: #d4b381;
            border-color: #d4b381;
        }
    </style>
</head>
<body>
    <a href="/" class="brand">ELIOR</a>
    <div class="tagline">Natural Stones</div>
    
    <div class="code">500</div>
    <h1 class="title">System Interruption</h1>
    <p class="desc">An unexpected error occurred while processing your request. Our technical team has been notified. No data or enquiry has been compromised.</p>
    
    <div class="actions">
        <a href="/" class="btn btn-primary">Return to Experience</a>
    </div>
</body>
</html>
